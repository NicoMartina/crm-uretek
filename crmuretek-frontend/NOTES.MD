useState is a variable that when changed re renders (re runs) the component in which that variable is used.

useEffect runs code when something changes. (stilll a bit confused)

.map() is THE way of that React renders (also creates?) lists. These lists need a key prop for fast lookups.
